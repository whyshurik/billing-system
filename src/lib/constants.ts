export enum UserStatuses {
  ACTIVATED = 'Activated',
DEACTIVATED = 'Deactivated',
  BLOCKED = 'Blocked'
}

export enum TransactionStatus {
  PENDING = 'Pending',
  SUCCESS = 'Success',
  FAILED = 'Failed',
  CANCELLED = 'Cancelled'
}

export enum Roles {
  Admin = 'Admin',
  Client = 'Client'
}

export enum AdminRoles {
  Admin = 'admin'
}